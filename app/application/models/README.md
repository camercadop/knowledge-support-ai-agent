# models

Infrastructure-free dataclasses that represent the core entities of the application layer.

These models are the return types of repository ports and are used by use cases. They must not import from `app/infrastructure/`.
